package ${packageName}.dao;

import ${packageName}.${modelName};
import cn.aixuegao.common.base.dao.CrudDao;

public interface ${modelName}Dao extends CrudDao<${modelName}>{
	//基本的增删改查只需要继承，以下直接写业务需要的方法	
}